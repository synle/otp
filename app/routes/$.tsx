import Index from "~/routes/_index";

/**
 * Catch-all route. Any URL that does not match a more specific route renders
 * the same UI as `/` so the SPA can take over client-side routing.
 */
export default Index;
